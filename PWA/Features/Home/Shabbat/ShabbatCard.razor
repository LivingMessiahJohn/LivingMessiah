@using PWA.Enums
@using PWA.Features.Home.Shabbat.Teaching
@using PWA.Features.Home.Shabbat.Teaching.Archives
@using RCL.Features.Parasha.Enums
@using RCL.Features.Parasha
@using ParashaEnums = RCL.Features.Parasha.Enums

<div class="card border border-danger border-3 rounded-top text-center mb-3 mt-3">

	<div class="card-header bg-danger">
		<h2 class="text-center text-white ">Shabbat</h2>
	</div>

	<div class="card-body fs-5 py-3 bg-primary-subtle rounded-1 m-1 ">
		Worship service begins at: <span class="fw-bold" title="Arizona Time zone">3:00 pm</span>
	</div>

	<div class="card-body fs-5 py-3 bg-primary-subtle rounded-1 m-1 ">
		<a class="btn btn-outline-primary btn-lg"
			 href="@Nav.Liturgy.Index" title="Go to the @Nav.Liturgy.Title wizard">
			<i class="@Nav.Liturgy.Icon"></i>
			@Nav.Liturgy.Title &nbsp; <i class="fas fa-chevron-right"></i>
		</a>
	</div>

	<div class="card border border-primary border-2 rounded-top my-1 mx-1">
		<ParashaHeader Triennial="@Triennial" />

		<div class="card-body bg-primary-subtle ">
			<div class="row g-1">

				<div class="col-4">
					<ButtonWrapper PdfType="@ParashaEnums.PdfType.CompleteService" Triennial="@Triennial" />
				</div>

				<div class="col-4">
					<ButtonWrapper PdfType="@ParashaEnums.PdfType.TeachingOnly" Triennial="@Triennial" />
				</div>

				<div class="col-4">
					<MarksWebsite />
				</div>

			</div>
		</div>

		<div class="bg-primary-subtle p-1 border-top border-dark-subtle">
			<Grid Triennial="@Triennial" />
		</div>

	</div>

</div>

@code {

	Triennial? Triennial;


	// Sync: current reading is a date-based lookup. ButtonWrapper / Grid are async for blob existence checks.
	// Null Triennial: buttons hidden; Grid still lists older PDFs (no current-week existence check).
	// Grid receives Triennial so it can list the current week only if either PdfType exists on Azure.
	protected override void OnInitialized()
	{
		Triennial = RCL.Features.Parasha.Helpers.GetCurrentReading();
	}
}
