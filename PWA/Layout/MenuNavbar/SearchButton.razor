
<a class="nav-link btn btn-link text-white ms-1"
	 data-bs-toggle="modal" data-bs-target="#menuNavbarSearchModal"
	 title="<Ctrl>+K @SearchModal.Title ">
	<i class="@SearchModal.Icon"></i>
</a>

@code {
	/*
	<a class="nav-link btn btn-link text-white @MarginEnd"
	<i class="@SearchModal.Icon @IconSize"></i>

	[Parameter, EditorRequired] public bool IsXs { get; set; }
	protected string IconSize => IsXs ? "" : " fa-fw fa-2x ";
	protected string MarginEnd => IsXs ? "me-3" : " me-2 ";
	*/
}
